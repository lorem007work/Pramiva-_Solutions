# Copy to .env.local and fill in.
# NOTE: this is a STATIC EXPORT. Anything here reaches the browser in plain
# text. Never put a secret in this file. SMTP credentials live in a PHP config
# OUTSIDE public_html on the server. See docs/ARCHITECTURE.md section 6.

# PLACEHOLDER: exact registered domain, still unknown. Baked into canonical
# URLs and the sitemap AT BUILD TIME, so set it before the production build.
NEXT_PUBLIC_SITE_URL=https://example.com
